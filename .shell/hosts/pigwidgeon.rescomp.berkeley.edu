### RUBY and GEMS take 2
# disabled in favor of RVM
# export GEM_HOME=$PREFIX/lib/ruby/gems/1.3.5

# OS X prefix
PREFIX=$HOME/prefixes/osx
PATH="$PREFIX/bin:$PATH"

# Use textmate as our editor
EDITOR="mate -w"

# use nvm to manage node like we use rvm to manage ruby
# source "$PREFIX/nvm/nvm.sh"

# Aliases
alias illustrator="sudo echo Starting Illustrator; sudo /Applications/Adobe\ Illustrator\ CS5/Adobe\ Illustrator.app/Contents/MacOS/Adobe\ Illustrator &"