# Grab screen EVERY TIME
# bad idea
# exec screen -DR
PREFIX="$HOME/prefixes/irc"
source "$PREFIX"/bin/activate
alias stealthirssi="irssi --home=~/.stealthirssi"

getgif() {
    wget -O ~/public_html/gif/"$2".gif "$1"
}
